spring:
  application:
    name: stock-service

server:
  port: 8082

grpc:
  server:
    port: 9090

integrations:
  order-engine:
    url: http://localhost:8080

simulation:
  cancellation-delay-seconds: 2
